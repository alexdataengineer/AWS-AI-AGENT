# Example: API Key Authentication
# Uncomment and configure to add API key authentication to your API
#
# resource "aws_apigatewayv2_api_key" "api_key" {
#   name = "${var.project_name}-api-key-${var.environment}"
# }
#
# resource "aws_apigatewayv2_api_mapping" "api_key_mapping" {
#   api_id      = aws_apigatewayv2_api.http_api.id
#   stage_id    = aws_apigatewayv2_stage.prod.id
#   api_key_id  = aws_apigatewayv2_api_key.api_key.id
# }
#
# Usage:
# After deployment, get the API key:
# terraform output -raw api_key_id
# aws apigatewayv2 get-api-key --api-key <key-id> --include-value
#
# Then include in requests:
# curl -H "x-api-key: <api-key-value>" https://<api-url>/chat
